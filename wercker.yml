box: wercker/default
build:
  steps:
    - virtualenv:
        venv
    - pip-install:
        requirements_file: "tests/dev-requirements.txt"
    - script:
        name: Python Information
        code: |
          echo "python version $(python --version) running"
          echo "pip version $(pip --version) running"